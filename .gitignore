# Per-user Claude Code local overrides (machine-specific paths, personal perms).
# Shared config lives in .claude/settings.json, which IS committed.
.claude/settings.local.json

# Personal meeting prep — not repo content.
MEETING-PREP.md
